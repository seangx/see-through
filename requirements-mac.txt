# Mac (Apple Silicon, MPS) requirements — Python 3.12
#
# Setup:
#   conda create -n see_through_mac python=3.12 -y
#   conda activate see_through_mac
#   pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0
#   pip install -r requirements-mac.txt
#
# vs upstream requirements.txt:
#   - drop bitsandbytes / NF4 paths (handled in -bnb tier upstream, not here)
#   - drop pillow-jxl-plugin (binary wheel troublesome on aarch64; reinstate if needed)
#   - keep transformers/diffusers pins from upstream
#   - PyQt6 kept (UI works on Mac via Qt6)

-e ./common
-e ./annotators

# --- Core scientific ---
numpy==2.2.6
opencv-python==4.13.0.92
pillow==12.1.1
matplotlib==3.9.4
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
scikit-image==0.25.2
einops==0.8.2

# --- HuggingFace stack ---
transformers==5.0.0
diffusers==0.37.0
huggingface-hub==1.7.2
tokenizers==0.22.2
accelerate==1.13.0
safetensors==0.7.0
timm @ git+https://github.com/huggingface/pytorch-image-models@6e3fdda39508db30766f9d9e6ec32380ebee8b8c
simple-parsing==0.1.8

# --- ML utilities ---
kornia==0.8.2
omegaconf==2.3.0
pycocotools==2.0.11
grad-cam==1.5.5

# --- Qt UI (optional, can be skipped if only running CLI) ---
PyQt6==6.9.1
PyQt6-Qt6==6.9.1
qtpy==2.4.3

# --- UI-specific ---
natsort==8.4.0
py7zr==1.1.0
termcolor==3.3.0
colorama==0.4.6
pyyaml==6.0.3

# --- Data & IO ---
click==8.3.1
psd-tools[composite]==1.14.2
tqdm==4.67.3

# --- VAE training (only needed for training; safe to omit for inference) ---
lpips==0.1.4
# convnext_perceptual_loss — only needed for VAE training, skip for Mac inference

# --- Dev tools ---
ipykernel==7.2.0
Cython==3.2.4
wheel==0.45.1
pytest==8.4.1
